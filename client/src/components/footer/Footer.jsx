import React from 'react';
import styles from './Footer.module.css';
const Footer = (props) => (
  <footer className={styles.footer}>
    <h1 className={styles.title}>MOBA</h1>
  </footer>
);
export default Footer;
